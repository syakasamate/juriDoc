{% extends 'base1.html.twig' %} {% block body %}

<div class="container e mt-5">
    <form class="container mt-5 " action="{{ path('login') }}" method="post">
        <div class="form-group">
            <label for=""></label>
            <input class="form-control" type="text" name="_username"  placeholder="Adresse Email....">
        </div>
        <div class="form-group">
            <label for=""></label>
            <input class="form-control"  type="password" name="_password" id="" class="form-control" placeholder="password...">
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-success"> connection</button>
        </div>
    </form>
</div>
{% if error %}
<div class="alert alert-danger">
    <ul class="list-unstyled mb-0">
        <li><span class="initialism form-error-icon badge badge-danger">Mot de Pass ou Email non valide</span>
        </li>
    </ul>
</div>
{% endif %} {% endblock %} {% block footer %} {% endblock %}